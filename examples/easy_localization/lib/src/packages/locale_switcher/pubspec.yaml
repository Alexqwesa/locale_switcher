name: locale_switcher
description: A small widget for switching the locale of your application.

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.7 

# Supported flags: [us, vn, de] 
